<div class="show-page">
  <div>
    <h1><%= @article.title %></h1>
    <%= link_to 'Edit', edit_article_path(@article.id) %>
    <%= link_to  article_path, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
      <i class="fa-solid fa-trash"></i>
    <% end %>
  </div>
  <p><%= @article.content %></p>
</div>
