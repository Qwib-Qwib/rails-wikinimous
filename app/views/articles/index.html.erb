<div class="articles-list">
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article.id), class: 'article-title' %></h2>
    <p class="article-content"><%= article.content %></p>
  <% end %>
</div>
